---
const features = [
    {
        title: "Automated Capture",
        description: "Automatically capture slides during Zoom presentations without manual intervention",
        icon: "📸"
    },
    {
        title: "AI Duplicate Detection",
        description: "Smart AI-powered detection removes duplicate slides automatically",
        icon: "🤖"
    },
    {
        title: "One-Click PDF",
        description: "Generate professional PDFs from your captured slides instantly",
        icon: "📄"
    },
    {
        title: "User-Friendly",
        description: "Simple interface designed for students, professionals, and educators",
        icon: "👥"
    }
];
---

<section class="features">
    <h2>Key Features</h2>
    <div class="feature-grid">
        {features.map(feature => (
            <div class="feature-card">
                <div class="icon">{feature.icon}</div>
                <h3>{feature.title}</h3>
                <p>{feature.description}</p>
            </div>
        ))}
    </div>
</section>

<style>
    .features {
        padding: 4rem 2rem;
        background: #f8f8f8;
    }

    h2 {
        text-align: center;
        margin-bottom: 3rem;
        font-size: 2.5rem;
        color: var(--accent);
    }

    .feature-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
        max-width: 1200px;
        margin: 0 auto;
    }

    .feature-card {
        background: white;
        padding: 2rem;
        border-radius: 12px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        text-align: center;
        transition: transform 0.2s;
        border: 1px solid #C8C8C8;
    }

    .feature-card:hover {
        transform: translateY(-5px);
        border-color: var(--accent);
    }

    .icon {
        font-size: 3rem;
        margin-bottom: 1rem;
    }

    h3 {
        color: var(--accent);
        margin-bottom: 1rem;
    }

    p {
        color: #666;
        line-height: 1.6;
    }
</style>